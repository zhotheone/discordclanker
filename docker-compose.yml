services:
  bot:
    build: .
    container_name: discordclanker
    restart: unless-stopped
    env_file: .env
    environment:
      - COOKIES_PATH=/app/www.youtube.com_cookies.txt
    volumes:
      - bot_data:/app/data
    networks:
      - internal

volumes:
  bot_data:

networks:
  internal:
    external: true
